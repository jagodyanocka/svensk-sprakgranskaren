[project]
name = "swedish-tutor"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "fastapi>=0.138.0",
    "gradio>=6.25.0",
    "openai>=3.3.1",
    "uvicorn>=0.49.0",
]
[tool.uv]
package = true
[project.scripts]
server = "app.run:main"
